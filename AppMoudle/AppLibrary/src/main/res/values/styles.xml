<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppThemeBase">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/blue_shallow</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:divider">@drawable/ic_list_divider</item>
        <item name="android:dividerHeight">@dimen/division_line</item>
        <item name="android:listDivider">@drawable/ic_list_divider</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_arrow_back_white_24dp</item>
        <item name="android:textColorPrimary">@android:color/white</item>
       <!-- <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>-->
        <!--<item name="styleLoadingLayout">@style/LoadingLayoutStyle</item>-->
    </style>

    <style name="myDialogTheme" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item><!--除去title-->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowBackground">@android:color/transparent</item><!--除去背景色-->
    </style>

    <style name="AppTheme.Toolbar" parent="AppTheme.Toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">@color/blue_shallow</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="actionMenuTextColor">@color/write</item>
    </style>

    <!--<style name="LoadingLayoutStyle" parent="LoadingLayout.Style">
        <item name="llEmptyImage">@drawable/ic_empty</item>
        <item name="llErrorImage">@drawable/ic_error</item>
        <item name="llLoadingResId">@layout/common_loading</item>
    </style>-->

    <style name="BottomNavigation">
        <!--整个控件的背景色，如果itemBackground没有设置正常状态下的背景色，则有效，否则无效，因为被覆盖了-->
        <item name="android:background">@color/colorPrimary</item>
        <!--item的背景色，可以设置按压下的水波纹效果，水泼纹效果要放置在drawable-v21下，或者在drawable下用targetApi约束-->
        <item name="itemBackground">@color/gray_small_double</item>
        <!--图标颜色-->
        <item name="itemIconTint">@color/bottom_bar_item_bg</item>
        <!--字体颜色-->
        <item name="itemTextColor">@color/bottom_bar_item_bg</item>
        <item name="paddingStart">50dp</item>
        <item name="paddingEnd">50dp</item>
    </style>

</resources>
